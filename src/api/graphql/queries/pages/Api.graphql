query ApiQuery($login: String!) {
  organization(login: $login) {
    repositories(first: 100) {
      nodes {
        id
        name
        url
        viewerHasStarred
        stargazers {
          totalCount
        }
      }
    }
  }
}
