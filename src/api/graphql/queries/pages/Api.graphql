query ApiQuery {
  organization(login: "facebook") {
    repositories(first: 100) {
      nodes {
        id
        name
        url
        viewerHasStarred
        stargazers {
          totalCount
        }
      }
    }
  }
}
